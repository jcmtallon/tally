import styled from 'styled-components'
import { TableSortLabel } from '../TableSortLabel'
import { TableCell } from '../TableCell'

const SortLabel = styled(TableSortLabel)``
const Cell = styled(TableCell)``

export { SortLabel, Cell }
