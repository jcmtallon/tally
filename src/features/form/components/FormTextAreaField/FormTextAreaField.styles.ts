import styled from 'styled-components'
import { Field as BaseField, TextArea as BaseTextArea } from 'components'

const Field = styled(BaseField)``
const TextArea = styled(BaseTextArea)``

export { Field, TextArea }
